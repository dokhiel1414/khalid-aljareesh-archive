/**
 * تبويب المرئيات — كل المواد المرئية.
 */

import { CategoryCatalog } from "@/features/catalog/CategoryCatalog";

export default function VideoTab() {
  return <CategoryCatalog category="VIDEO" />;
}
