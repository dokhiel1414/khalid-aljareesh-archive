/**
 * تبويب الصوتيات — كل المواد الصوتية (دروس ومحاضرات).
 */

import { CategoryCatalog } from "@/features/catalog/CategoryCatalog";

export default function AudioTab() {
  return <CategoryCatalog category="AUDIO" />;
}
